'use strict';

module Rx
{
    //export interface IObservable<T>
    //{
    //    $subscribe($scope, callback);
    //    $bindTo($scope, prop: string);
    //}
    //
    //export interface Observable<T>
    //{
    //    $subscribe($scope, callback);
    //    $bindTo($scope, prop: string);
    //}
    //
    //export interface ObservableStatic
    //{
    //    prototype: any;
    //}
    //
    //Observable.prototype.$subscribe = function ($scope: ng.IScope, callback)
    //{
    //    var subs = this.subscribe((data) =>
    //    {
    //        $scope.$evalAsync(() => callback(data));
    //    });
    //
    //    $scope.$on('$destroy', () => subs.dispose());
    //};
    //
    //Observable.prototype.$bindTo = function ($scope: ng.IScope, prop)
    //{
    //    this.$subscribe($scope, (data) => $scope[prop] = data);
    //};
}

